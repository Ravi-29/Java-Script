const arr = [0, 1, 2, 3, 4]

const arr2 = ["Shaktiman","GendaSwami"]
const arr3 = new Array(1,2,3,4)

console.log(arr[2]);


//  Array Methods

// arr.push(5,6,7);
// arr.pop()

//arr.unshift(9)  // add value at first index
//arr.shift() // remove the first index value

// console.log(arr.includes(7));  // to find value present in array or not
// console.log(arr.indexOf(0));

const newArr = arr.join()   // Change the value in string
console.log(arr);
console.log(typeof newArr);